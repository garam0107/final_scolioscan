# Scolioscan Project Instructions

이 파일은 Scolioscan 프로젝트에서 작업을 수행할 때 반드시 준수해야 하는 지침입니다. `agents.md`에 정의된 Codex 작업 규칙을 기반으로 작성되었습니다.

## 프로젝트 개요
- **Frontend**: React Native + Expo (주요 작업 범위)
- **Backend**: Python + FastAPI
- **주요 작업**: Figma 디자인 반영, 화면 구현, API 연동, UI 버그 수정

## 핵심 원칙 (Core Principles)
1. **최소 변경 원칙**: 요청 범위를 벗어나는 리팩토링은 하지 않으며, 기존 동작을 바꾸는 수정은 최소화합니다.
2. **사전 분석**: 변경 전 관련 파일과 영향 범위를 먼저 파악합니다.
3. **주석 작성**: 
   - **반드시 한글로 작성**해야 합니다. (영어 금지)
   - 코드 수정 후 유지보수를 위해 상세히 남깁니다.
   - 수정이 필요하지 않은 기존 코드나 주석은 절대 건드리지 않습니다.
4. **의존성 관리**: 사용자가 요청하지 않은 의존성 추가나 패키지 구조 재편은 금지합니다.
5. **승인 후 수정**: 코드를 수정하기 전에 계획을 확인받고 진행합니다.

## UI/UX 및 디자인 규칙
- **Figma 일치**: 레이아웃, 간격, 색상, Border Radius, 타이포그래피를 Figma 디자인과 정확히 일치시킵니다.
- **반응형 디자인**: 다양한 모바일 기기 비율에서 동일하게 보이도록 구현합니다.
- **컴포넌트 재사용**: 기존 컴포넌트와 에셋을 최대한 활용하고, 반복되는 UI는 공통 컴포넌트로 추출합니다.
- **디자인 토큰 사용**:
  - `tokens/*.json` 또는 기존 테마/토큰 모듈의 값을 사용합니다.
  - 색상, 간격 등 UI 값을 하드코딩하지 마십시오. (예: `#fff`, `rgb()` 대신 토큰 사용)

## 개발 표준
- **에러 처리**: 에러는 무시하지 않고 명확하게 처리합니다.
- **Import**: 공식 문서 기준의 정확한 Import 방식을 사용합니다.
- **테스트**: 테스트가 실패한 상태로 작업을 완료하지 않습니다.
- **Lint**: 작업과 관련 없는 Lint 수정은 지양합니다.

## 작업 및 보고 절차
1. **요구사항 정리**: 원인 또는 요구사항을 짧게 요약합니다.
2. **구현**: 영향이 적은 방식으로 구현하며, 필요한 경우에만 새 파일을 추가합니다.
3. **검증**: 수정 후 검증 결과를 정리합니다.
4. **응답 형식**: 최종 응답에는 다음이 포함되어야 합니다.
   - 작업 요약
   - 변경된 파일 목록
   - 핵심 수정 내용
   - 검증 결과
   - 남은 리스크 및 확인 사항

## 디자인 QA (Design QA)
- 디자인 QA는 `qa-orchestrator` 및 세부 에이전트(`code-scanner`, `token-auditor` 등)를 통해 수행될 수 있습니다.
- QA 결과(실패, 불일치 등)는 파일 경로와 근거를 함께 보고합니다.
- 사용자의 별도 승인 전에는 QA 결과에 따른 자동 수정을 진행하지 않습니다.
